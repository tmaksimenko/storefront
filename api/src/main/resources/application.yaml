server:
  port: 8080
  error:
    include-stacktrace: never

logging.level:
  org.springframework.security: trace

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/storefront_schema
    username: root
    password: testpass
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true

