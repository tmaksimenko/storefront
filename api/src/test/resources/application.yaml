logging.level:
  org.springframework.security: trace

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/storefront_test
    username: root
    password: testpass
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
